<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
<% if current_user && current_user.image.attached? %>
    <%= link_to admin_user_path(current_user) do %>
      <%= image_tag current_user.image, alt: "User Image", class: "user-image rounded-circle" ,style:"width:47px; height:50px;" %>
     
    <% end %>
  <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
       
          <%= link_to "Home", root_path, class: "nav-link" %>

      </li>
    
        <li>
       
     <% if user_signed_in? && current_user.has_role?('admin') %>
        <%= link_to "Products", productdetails_path,class: "nav-item nav-link" %>
        <%#= link_to "Log out", destroy_user_session_path,class: "nav-item nav-link" %>
        <%#= link_to "log out", destroy_user_session_path, data: { method: :delete }, class: 'nav-item nav-link'  %>
          
        <% end %>
 
      </li>
     
<li class="nav-item">
            <% if !user_signed_in? %>
       <%= link_to "Login", new_user_session_path, class:"nav-item nav-link" %>
       <% end %>
       </li>
        <li class="nav-item">
            <% if user_signed_in? %>
       <%= link_to "Log out", destroy_user_session_path, data: { method: :delete }, class: 'nav-item nav-link'  %>
       <% end %>
       <li class="nav-item">
            <% if user_signed_in? && current_user.has_role?('admin') %>
       <%= link_to "Vendor",vendors_path,  class: 'nav-item nav-link'  %>
       <% end %>
       </li>
         <li class="nav-item">
            <% if user_signed_in? && current_user.has_role?('admin') %>
       <%= link_to "Edit profile",edit_admin_user_path(current_user),  class: 'nav-item nav-link'  %>
       <% end %>
       </li>
      <li class="nav-item">
        <% if !user_signed_in? %>
       <%= link_to "Sign up", new_user_registration_path, class:"nav-item nav-link" %>
       <% end %>
       </li>
      
      <li class="nav-item">
        <a class="nav-link disabled" href="#">Disabled</a>
      </li>
    </ul>
<div class="form-inline my-2 my-lg-0">
<%= form_tag productdetails_path,  method: :get, remote: true, html: { id: 'search-form' } do %>

  <%= text_field_tag :product_title_or_vendor_first_name_cont, params[:product_title_or_vendor_first_name_cont], placeholder: 'Search by Product Title ', class: 'search form-control mr-sm-2' %>
  <%= submit_tag nil, class: 'btn btn-outline-success my-2 my-sm-0 ', style: 'margin-right: 36px;' do %>
<i class="fa fa-search"></i> Search

<% end %>
</div>
   
  </div>
  

  </div>
<% end %>
</nav>